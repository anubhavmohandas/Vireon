# Vireon dependencies — fully standalone, no SAGE needed
#
# Install:
#   pip install -r requirements.txt

# ── LLM (OpenAI-compatible client — works with Featherless, AIML, Anthropic) ──
openai>=1.30.0

# ── Band SDK ──────────────────────────────────────────────────────────────────
band-sdk[anthropic]

# ── FastAPI backend ───────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.30.6
httpx==0.27.0

# ── Core dependencies ─────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
networkx>=3.0
rich>=13.5.2
packaging>=23.0
